/**
 * Dominio del modulo de identidad (usuarios y roles). Sin dependencias de Spring, JPA ni infraestructura.
 */

@Application
package com.fleetbite.identity.domain;

import org.jmolecules.architecture.hexagonal.Application;
